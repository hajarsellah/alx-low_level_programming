#include <stdio.h>
/**
  *main - Entry point
  *Description: 'get the last degiti of a number'
  *Return: always 0
  */
int main(void)
{
int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
return (0);
}
